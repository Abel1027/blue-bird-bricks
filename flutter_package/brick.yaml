name: flutter_package

description: Create a Flutter package using a clean architecture pattern

version: 0.1.0+1

environment:
  mason: ">=0.1.0-dev.40 <0.1.0"

vars:
  package_name:
    type: string
    description: Package name
    default: bb_flutter_package
    prompt: "Name of Flutter package:"
